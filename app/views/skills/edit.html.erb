<% provide(:button, 'Update') %>
<div class="container-fluid">
    <h2 class="text-center">Update Skill</h2>
    <div>
    <ul>
        <% @skill.errors.full_messages.each do |error| %>
            <li class="alert alert-danger"><%= error %></li>
        <% end %>
        </ul>
    </div>

    <div class="container">
        <%= render 'form' %>
    </div>
</div>