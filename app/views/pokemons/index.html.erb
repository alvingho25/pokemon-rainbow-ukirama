<div class="container-fluid">
    <div class="clearfix">
        <h2>Pokemon</h2>
        <%= link_to "New Pokemon", new_pokemon_path, class: "btn btn-primary float-right"%>
    </div>

    <table class="table table-bordered" style= "margin-top:10px">
        <thead>
            <tr class="d-flex">
                <th class="col-3 text-center">Name</th>
                <th class="col-3 text-center">Level</th>
                <th class="col-3 text-center">Health</th>
                <th class="col-3 text-center">Action</th>
            </tr>
        </thead>
        <tbody>
            <% @pokemons.each do |pokemon| %>
                <tr class="d-flex">
                    <td class="col-3 text-center"><%= pokemon.name %></td>
                    <td class="col-3 text-center"><%= pokemon.level %></td>
                    <td class="col-3 text-center"><%= pokemon.current_health_point %>/<%= pokemon.max_health_point %></td>
                    <td class="col-3 text-center">
                    <%= link_to "Show", pokemon_path(pokemon), class: "btn btn-info"%>
                    <%= link_to "Delete", pokemon_path(pokemon), method: :delete,
                                    data: { confirm: "You sure?" }, class: "btn btn-danger"%>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>
    <div class="clearfix">
        <%= will_paginate @pokemons, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer, class: "float-right" %>
    </div>
</div>